quarkus.application.name=feedback-intake-fn

# ---------------------------------------------------------------------------
# HTTP / API
# Os recursos REST declaram o prefixo /api/v1; a raiz permanece em "/" para
# manter os endpoints de gestao (/q/health, /q/openapi) nos caminhos padrao.
# ---------------------------------------------------------------------------
quarkus.http.port=8080

# ---------------------------------------------------------------------------
# DynamoDB
# Em producao a tabela e criada pela infraestrutura como codigo (SAM) e as
# credenciais vem da role da funcao Lambda. Em dev/test o Quarkus sobe um
# container do DynamoDB Local via Dev Services (Testcontainers).
# ---------------------------------------------------------------------------
app.dynamodb.tabela=${FEEDBACK_TABLE:feedback}
quarkus.dynamodb.aws.region=${AWS_REGION:us-east-1}
quarkus.dynamodb.sync-client.type=url
%prod.quarkus.dynamodb.aws.credentials.type=default
%dev.quarkus.dynamodb.devservices.enabled=true
%test.quarkus.dynamodb.devservices.enabled=true

# ---------------------------------------------------------------------------
# SNS: topico de feedbacks criticos consumido pela funcao de notificacao
# ---------------------------------------------------------------------------
app.sns.topico-feedback-critico=${FEEDBACK_CRITICO_TOPIC_ARN:arn:aws:sns:us-east-1:000000000000:feedback-critico}
quarkus.sns.aws.region=${AWS_REGION:us-east-1}
quarkus.sns.sync-client.type=url
%prod.quarkus.sns.aws.credentials.type=default
%dev.quarkus.sns.devservices.enabled=true
%test.quarkus.sns.devservices.enabled=true

# ---------------------------------------------------------------------------
# SES: esta funcao nao envia e-mail, mas compartilha o modulo core que o declara
# ---------------------------------------------------------------------------
app.ses.remetente=${SENDER_EMAIL:no-reply@example.com}
app.ses.administradores=${ADMIN_EMAILS:admin@example.com}
quarkus.ses.aws.region=${AWS_REGION:us-east-1}
quarkus.ses.sync-client.type=url
%prod.quarkus.ses.aws.credentials.type=default
%dev.quarkus.ses.devservices.enabled=true
%test.quarkus.ses.devservices.enabled=true

# ---------------------------------------------------------------------------
# OpenAPI
# ---------------------------------------------------------------------------
mp.openapi.extensions.smallrye.info.title=Feedback Management API
mp.openapi.extensions.smallrye.info.version=1.0.0
mp.openapi.extensions.smallrye.info.description=Plataforma serverless de feedback de aulas - Tech Challenge FIAP Fase 4
quarkus.swagger-ui.always-include=false

# ---------------------------------------------------------------------------
# Logs
# Em producao (Lambda/CloudWatch) usamos JSON estruturado; em dev/test o
# console legivel facilita o desenvolvimento.
# ---------------------------------------------------------------------------
quarkus.log.level=INFO
quarkus.log.category."br.com.fiap.feedback".level=DEBUG
%dev.quarkus.log.console.json.enabled=false
%test.quarkus.log.console.json.enabled=false

# ---------------------------------------------------------------------------
# AWS Lambda (API Gateway HTTP API)
# Em dev/test o mock event server do Quarkus traduz requisicoes HTTP comuns em
# eventos do API Gateway, permitindo usar RestAssured e Newman normalmente.
# Mantido habilitado (default); desabilitar interrompe o poll loop e derruba a
# aplicacao em modo de teste.
# ---------------------------------------------------------------------------
quarkus.lambda.mock-event-server.enabled=true

# ---------------------------------------------------------------------------
# Cobertura de testes
# A extensao grava no mesmo arquivo do agente do Maven (reuse-data-file evita
# que ele seja apagado); o relatorio e o gate de 80% ficam com o jacoco-maven-plugin.
# ---------------------------------------------------------------------------
%test.quarkus.jacoco.data-file=target/jacoco.exec
%test.quarkus.jacoco.reuse-data-file=true
%test.quarkus.jacoco.report=false
